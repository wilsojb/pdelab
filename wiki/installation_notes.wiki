===Installation Notes===
----

The pdelab binary should be located in the bin directory after executing the following build from the root pdelab directory:

make clean

make

Before you execute this build, you must have the g++ (gcc) compiler.  This build has only been tested on a Mac OS X.  If you have problems with installation please post it under the Issues tab.